[project]
name = "iceberg-entities"
version = "0.1.0"
description = "Capa de acceso a datos — modelos SQLModel (tablas)"
requires-python = ">=3.12"
dependencies = ["sqlmodel>=0.0.22"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/iceberg_entities"]
